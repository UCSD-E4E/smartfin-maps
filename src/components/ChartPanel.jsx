import React from 'react'

export default function ChartPanel({ session }) {


  return (
    <div id="chart-panel-wrapper">
      <header>
        {session.id}
      </header>
      <main>
        {session.id}
      </main>
    </div>
  )
}
